# === OS/Editor Files ===
.DS_Store           # macOS folder attributes
Thumbs.db           # Windows thumbnail cache
.idea/              # JetBrains IDE (PHPStorm)
.vscode/            # VS Code settings (excluding essential configs)
*.swp               # Vim swap files

# === Dependency Folders ===
/vendor/            # Composer (PHP)
/node_modules/      # Node.js (if using frontend tools)
/bower_components/  # Bower (legacy)

# === Environment/Configs ===
.env                # Environment variables (API keys, DB credentials)
*.env.*             # Environment variants (e.g., .env.local)
/config.php         # Sensitive config files (exclude or use .env)
/secrets/           # Folder for sensitive data

# === Logs/Cache ===
*.log               # Error logs
/logs/              # Log directory
/cache/             # PHP cache (e.g., Twig, Opcache)

# === Uploads/User Content ===
/uploads/           # User-uploaded files (images, PDFs)
/tmp/               # Temporary files

# === Build Artifacts ===
/dist/              # Compiled CSS/JS (if using Sass, Webpack)
*.min.css           # Minified CSS (if source files are tracked)
*.min.js            # Minified JS

# === System Files ===
.htaccess           # Apache config (if project-specific, exclude)
*.sql               # Database dumps (store schema separately)
